# Feedback

This file is durable review memory. Capture the author's actual observation,
not a generalized rewrite of it. A note is resolved only after reviewing the
resulting cut.

## Open

Agents may add a readable unbound item directly as
`- [ ] **short-id** — [blocker] what must change`. Use `./chalk note add` when
the segment, timecode, snapshot, and exact cut should be bound automatically.

## Resolved

## Decisions
